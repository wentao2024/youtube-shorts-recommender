services:
  - type: web
    name: youtube-shorts-recommender
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd src/api && uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0

